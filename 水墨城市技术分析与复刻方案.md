# 《水墨杭州》技术分析与「水墨普宁」复刻方案

> 分析对象：
> - 你看的视频：BV1Yku16CEVz《水墨杭州 Three.js 复刻》，UP主「探幽侯月」，技术栈 **Vue + Three.js**（学习用途，复刻自下述原版）
> - 原版：BV1yG411Z7sw《three.js 实现水墨杭州迎亚运》，UP主 **gamemcu**，在线体验 https://gamemcu.com/hz
>
> 本文档结论基于对 gamemcu.com/hz 线上生产代码（index-4ed09a3d.js, ~1.58MB）的直接逆向分析，而非猜测。

---

## 一、原版《水墨杭州》到底用了什么技术

### 1. 总体架构

```
离线资产管线（Blender + GIS 数据）          运行时（Web 前端）
┌─────────────────────────┐          ┌──────────────────────────────┐
│ OSM/地图数据 → Blender   │          │ React + 自研 viewer 封装       │
│ 建筑白模/道路/地形        │   ──→    │ Three.js + 22 种自定义材质     │
│ 分块烘焙导出 GLB          │          │ 纸纹背景 + 雾 + 描边 + 水体着色器│
└─────────────────────────┘          └──────────────────────────────┘
```

核心思路一句话：**城市不是运行时从地图服务加载的，而是提前在 Blender 里做好白模、切成 189 个 GLB 分块，前端 Three.js 只负责用"水墨风着色器"把这些白模渲染出来。**

### 2. 逆向分析发现的资产清单（实锤证据）

线上包中引用的全部模型（共 189 个 GLB）：

| 类别 | 文件 | 数量 | 说明 |
|---|---|---|---|
| 建筑分块 | `map001_buildings.001.glb` ~ `.XXX.glb` | ~160 | 按 tile 切块的建筑白模，Blender 集合命名风格（`map001_buildings`） |
| 道路分块 | `map001_roads_secondary.001.glb` ~ `.020.glb` | 20 | 道路也是 3D 几何体（路面条带），非贴图 |
| 手工地标 | `SM_LeiFengTa.glb`（雷峰塔）、`SM_AoTi_Building60.glb`（奥体中心）、`SM_ChengShiYangTai.glb`（城市阳台）、`SM_HuangLong_Building111.glb`（黄龙）、`SM_ShangCheng.glb`（上城） | 5 | 重要建筑单独精修 |
| 地形 | `SM_HangZhouLandScape.glb` | 1 | 整块地形+水体网格（缩放 0.1 加载） |
| 动效 | `AN_Bird.glb`、`SM_Face.raw.glb` | 2 | 飞鸟动画等演出元素 |

全部纹理：

| 纹理 | 用途 |
|---|---|
| `T_Paper.png` | **宣纸底纹，直接作为 scene.background**（UV repeat=18 平铺） |
| `T_Height.jpg` | 地形高度图 |
| `T_RoadEdgeMask.png` | 道路边缘晕染遮罩（做出墨线渗开的感觉） |
| `T_Roof.png` | 屋顶水墨贴图 |
| `T_Wall01.png` | 墙面贴图 |
| `T_Water_BaseColor.png` / `T_Water_N.png` / `T_WaterEdge.png` | 水体：基色 + 法线 + 水墨边缘 |
| `T_TreeA/B/C.png` | 三种树的水墨画 sprite（广告牌） |
| `FNT_xingcao` | **草书字体**（页面标题的毛笔字） |
| `bmfont` | 位图字体（UI 文字） |

代码层面的着色器证据：
- `fragmentShader` × 79、`vertexShader` × 74、`ShaderMaterial` × 22 —— 大量自定义着色器
- 噪声相关：`noise` × 84、`fbm` × 11（fBm 分形噪声，用于笔触抖动/纸张颗粒）
- `edge` × 53（描边/边缘处理）
- `MeshToonMaterial` × 9、`EdgesGeometry` × 2、`LineSegments` × 6（卡通道具 + 几何描边）
- `fogExp2` × 9（指数雾 → 远处淡出成"留白"）
- `InstancedMesh` × 13、Sprite × 42（树木/道具批量实例化）

### 3. 水墨效果的六个组成要素（关键！）

水墨风 ≠ 一个滤镜，而是**多个手段叠加**：

1. **宣纸底**：整页背景是一张可平铺的宣纸纹理（T_Paper），不是纯白。所有元素的"白"其实是纸的白，物体本体也大量用"留白"（近白材质），只靠**墨线轮廓**定义形体——这是国画的核心审美。
2. **墨色浓淡 = 雾**：`fogExp2` 指数雾把远处建筑逐渐"融"进纸底，模拟墨分五色的浓淡过渡和画面的虚实。雾色就是纸色，所以远山远景自然变成淡墨晕染。
3. **描边**：建筑靠轮廓墨线定义。原版混合了两种路线：`EdgesGeometry + LineSegments`（几何棱边画线）和着色器边缘处理（噪声扰动让线有笔触感，`noise`/`fbm` 高频使用即为此）。
4. **道路/水系 = 画出的墨线**：道路是 3D 条带几何 + 边缘遮罩贴图（T_RoadEdgeMask）做出"墨线两边自然晕开"；水体是自定义 shader（基色+法线波动+边缘墨线 T_WaterEdge），留白多、笔意重。
5. **树木/植被 = 水墨画片**：树不是 3D 模型，而是三张水墨树画的 Sprite/Billboard + InstancedMesh 批量撒点，远景尤佳。
6. **演出细节**：草书字体标题、飞鸟动画（AN_Bird）、地形高度图起伏，以及相机运镜（镜头像在画中游）。

---

## 二、复刻「水墨普宁」实现路线

普宁（广东揭阳代管县级市，练江/榕江流域，以流沙市区和洪阳古城为主体）地图数据在 OpenStreetMap 上有一定覆盖，核心城区可用。下面按阶段给方案，**技术栈建议与那个复刻视频一致：Vue 3 + Vite + Three.js**，生态成熟、教程多。

### 阶段 0：定范围（半天）

- 先只做 **3km × 3km** 左右：建议「流沙市中心 + 流沙大道 + 练江/白坑湖一带」或「洪阳古城（德安里、普宁学宫、城隍庙）」二选一。范围越小越快出效果，再扩。
- 收集地标清单（手工建模对象）：
  - 洪阳线：德安里民居群、普宁学宫、城隍庙、培风塔
  - 流沙线：流沙大道、普宁广场、商品城、莲花山/盘龙阁（可远景区处理）
  - 特色彩蛋：**英歌舞**动画角色脸谱（SM_Face 原版也有类似人面演出）、潮汕"下山虎/四点金"民居屋顶造型（比雷峰塔好建模，且辨识度极高）

### 阶段 1：拿数据（1~2 天）

三条路任选，可组合：

1. **Overpass API（免费，推荐起点）**：在 https://overpass-turbo.eu 用查询语句框选普宁区域，导出 GeoJSON：
   ```
   建筑:  way["building"]({{bbox}});
   道路:  way["highway"]({{bbox}});
   水系:  way["natural"="water"],way["waterway"]({{bbox}});
   绿地:  way["leisure"="park"],way["landuse"~"grass|forest"]({{bbox}});
   ```
2. **Blender + BlenderGIS / blender-osm 插件**：直接在 Blender 里输入经纬度拉取 OSM 建筑/道路并自动挤出成 3D 楼块（原版管线大概率就是这类流程，`map001_buildings` 就是典型的 Blender 集合命名）。blender-osm（免费版够用，Pro 版可直接带高度和高精地形）。
3. **平面底图参考**：导出一片区域的地形高度（可用 ALOS 30m DEM 或 BlenderGIS 自带的 SRTM），配合 T_Height 式高度图。

> 普宁 OSM 建筑覆盖可能不全：先去 https://www.openstreetmap.org 搜"普宁"确认。缺的地方可以用「 edi map / 天地图轮廓 + 手动描建筑底面」补齐，或者干脆按"重点区域精细、周边示意"的国画构图来处理——水墨风本来就重神不重形，这是它相对写实城市 demo 的巨大优势。

### 阶段 2：Blender 离线烘焙（3~7 天）

1. 导入 OSM 数据 → 按区域**挤出建筑体块**（高度取 OSM `building:levels` 或按类型给默认层数；没有数据就统一 3~6 层随机）。
2. 屋顶可轻微分缝/给 T_Roof 式水墨贴图；墙体**保持白模**（水墨靠描边不靠材质）。
3. 道路：曲线转成有宽度的条带网格（路面平面即可），UV 横向展开以便运行时用边缘遮罩做晕染。
4. 水系：单独平面网格；地形：低多边形起伏网格。
5. **分块导出**：按 1km² 网格把建筑/道路拆成集合，命名仿照原版（`map001_buildings.001.glb`…），GLB 导出（可开 Draco 压缩，配 three.js 的 DRACOLoader）。分块是为了**按相机位置流式加载**，3km 范围其实也可以不分块，先跑通再说。
6. 地标精修：德安里/学宫/下山虎民居用低模 + 坡屋顶，几十个面就够，轮廓对了水墨味就出来了。

### 阶段 3：Three.js 场景搭建（3~5 天）

初始化清单（对应原版的每一项）：

```js
// 1. 宣纸背景（不是纯白！）
const paper = await loadTexture('T_Paper.png');
paper.wrapS = paper.wrapT = THREE.RepeatWrapping;
paper.repeat.set(18, 18);
scene.background = paper;

// 2. 指数雾 → 墨色浓淡与留白
scene.fog = new THREE.FogExp2(0xf5f1e6, 0.0035); // 雾色≈纸色

// 3. 建筑：白模 + 描边
const mat = new THREE.MeshBasicMaterial({ color: 0xffffff }); // 或浅灰渐变
// 描边方案A（简单可靠）：EdgesGeometry 棱边线
// 描边方案B（更水墨）：背面外扩法(inverted hull) + 噪声扰动顶点做笔触抖动
```

其他要点：
- **道路**：MeshBasicMaterial 白/浅灰 + 自制 `T_RoadEdgeMask` 风格边缘遮罩（可用 Canvas 程序生成：中间实、边缘渐隐），叠在建筑下方。
- **水**：PlaneGeometry + 自定义 ShaderMaterial——浅墨色基色、时间驱动的法线/噪声轻微扰动波纹、边缘画一圈深墨（用 uv 到边距离或 T_WaterEdge 式贴图）。
- **树**：找 3 张水墨树 PNG（或自己 AI 生成/画），`THREE.Sprite` 或面片 + `InstancedMesh` 沿路/公园撒点。
- **鸟**：简单两三角形翅膀扇动动画，绕镜头飞，画龙点睛且几乎零成本。
- **文字**：标题用毛笔字体（免费商用的「站酷快乐体/演示悠然小楷/所知字」等，或转成位图字），竖排落款 + 红印章，水墨味立刻翻倍。

### 阶段 4：水墨着色器（2~4 天，灵魂部分）

给描边和纸面加"笔触感"的通用配方（fBm 噪声扰动）：

```glsl
// value noise + fbm
float hash(vec2 p){ return fract(sin(dot(p, vec2(127.1,311.7))) * 43758.5453); }
float noise(vec2 p){
  vec2 i = floor(p), f = fract(p);
  vec2 u = f*f*(3.0-2.0*f);
  return mix(mix(hash(i), hash(i+vec2(1,0)), u.x),
             mix(hash(i+vec2(0,1)), hash(i+vec2(1,1)), u.x), u.y);
}
float fbm(vec2 p){
  float v = 0.0, a = 0.5;
  for(int i=0;i<4;i++){ v += a*noise(p); p *= 2.0; a *= 0.5; }
  return v;
}

// 描边片元：用 fbm 扰动 alpha，线条出现粗细枯湿变化
// float ink = smoothstep(0.3, 0.6, lineMask + (fbm(uv*40.0)-0.5)*0.35);
// 纸面颗粒：全屏叠加 fbm(uv*resolution*0.5) 的细颗粒，透明度 3~5%
```

进阶演出（可选）：入场时用溶解贴图 + fbm 阈值做"**晕染式浮现**"（建筑从纸里洇出来），这是水墨 demo 最出彩的镜头语言。

### 阶段 5：运镜、交互与发布（2~3 天）

- 相机：开场自动飞行路线（写样条曲线即可），随后允许拖拽/缩放（OrbitControls）。
- 加载进度：水墨圆研墨动画。
- 性能：建筑合并几何 / InstancedMesh；树用实例化；Draco 压缩 GLB；`powerPreference: 'high-performance'`；移动端降雾距降像素比。
- 发布：Vite 打包静态托管即可（原版就是纯静态站）。

### 时间与工具总结

| 工具 | 用途 | 费用 |
|---|---|---|
| Overpass Turbo / BlenderGIS | 拉普宁 OSM 数据 | 免费 |
| Blender 4.x + blender-osm | 建筑挤出、道路、分块导出 GLB | 免费 |
| Vue 3 + Vite + three.js (r160+) | 前端渲染 | 免费 |
| postprocessing / three 自带 EffectComposer | 可选后处理 | 免费 |
| Krita/PS 或 AI 生图 | 宣纸纹理、水墨树、印章 | 免费/自备 |

纯业余时间预估 2~4 周可出第一版可交互 demo（数据顺利、范围 3km² 的前提下）。

---

## 三、可以抄作业的现成轮子

- **blender-osm / BlenderGIS**：OSM → Blender 白模自动化，原版管线同款思路
- **three.js 官方OutlineEffect**（`examples/jsm/effects/OutlineEffect.js`）：toon 材质描边一步到位
- **OSM Buildings（osmbuildings.org，开源）**：Web 端 OSM 建筑挤出参考实现
- **three-geo**：经纬度范围 → 三维地形+卫星纹理
- **GitHub 搜 "three.js ink / 水墨"**：有多个水墨风 shader 开源 demo（纸张噪声、晕染转场）可直接借鉴
- 原版体验站：https://gamemcu.com/hz （F12 打开 Network 可直接看到本文提到的全部资产，是最好的"参考答案"）
